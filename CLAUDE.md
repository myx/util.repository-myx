# CLAUDE.md — util.repository-myx

The repo list this tooling clones/updates lives in `sh-data/repository/remotes-list-myx.txt` — the authoritative `<local-path> <git-remote-url>` pairs for a given workspace. Only a subset of the full set of `myx`-namespace repos may be listed here for any one workspace — check this file before assuming a `myx` repo doesn't exist anywhere.
